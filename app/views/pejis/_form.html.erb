<%= form_for(peji) do |f| %>
  <% if peji.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peji.errors.count, "error") %> prohibited this peji from being saved:</h2>

      <ul>
      <% peji.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="field">
    <%= f.label :manga_id %>
    <%= f.number_field :manga_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
