<%= form_for(magazine) do |f| %>
  <% if magazine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(magazine.errors.count, "error") %> prohibited this magazine from being saved:</h2>

      <ul>
      <% magazine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titre %>
    <%= f.text_field :titre %>
  </div>

    <div class="field">
      <%= f.label :thumbnail %>
      <%= f.file_field :thumbnail %>
    </div>

    <div class="field">
      <label for="images[]">Multi Upload tavu</label><br />
      <%= file_field_tag 'images[]', type: :file, multiple: true %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
