<p id="notice"><%= notice %></p>

<p>
  <strong>Titre:</strong>
  <%= @chapter.titre %>
</p>

<p>
  <strong>Apercu:</strong>
  <%= image_tag @chapter.apercu, size: '200x200' %>
</p>

<p>
  <% @chapter.pejis.each do |p| %>

      <%= link_to chapter_peji_path(id: p.scan_number, chapter_id: @chapter.slug) do %>
          <%= image_tag p.image, size: '100x100' %>
      <% end %>
  <% end %>
</p>

<%= link_to 'Edit', edit_chapter_path(@chapter) %> |
<%= link_to 'Back', chapters_path %>
